export enum RuleFormControl {
  Facts = 'facts',
  Conjunctions = 'conjunctions',
  Consequence = 'consequence',
}

export enum RuleFactFormControl {
  Name = 'name',
  Operation = 'operation',
  Value = 'value',
}

export enum RuleConsequenceFormControl {
  TaskId = 'taskId',
  TaskName = 'taskName',
  SkedDay = 'skedDay',
  SkedTime = 'skedTime',
}
